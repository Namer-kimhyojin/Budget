export function buildLedgerHeaderProps({
  version,
  isVersionEditable,
  selectedDeptName,
  selectedVersionPeriod,
  embeddedMode,
  openRoundSelectModal,
  incTotal,
  expTotal,
  isBalanced,
  diffTotal,
  inputProgressRatio,
  enteredCount,
  totalEditableCount,
  onRefresh,
  modalApi,
  exportToExcel,
  pushToast,
  versionLockMessage,
  setIsAddOpen,
  versionStatusLabel,
  user,
  authAxios,
  onVersionStatusChange,
  showMyEntries,
  setShowMyEntries,
  hiddenCols,
  toggleCol,
  applyColPreset,
  showTransferColumns,
  showSyncControls,
  isSyncRefreshing,
  lastSyncedAt,
  lastConflictAt,
  autoRefreshEnabled,
  setAutoRefreshEnabled,
  onRefreshNow,
}) {
  return {
    version,
    isVersionEditable,
    selectedDeptName,
    selectedVersionPeriod,
    embeddedMode,
    openRoundSelectModal,
    incTotal,
    expTotal,
    isBalanced,
    diffTotal,
    inputProgressRatio,
    enteredCount,
    totalEditableCount,
    onRefresh,
    modalApi,
    exportToExcel,
    pushToast,
    versionLockMessage,
    setIsAddOpen,
    versionStatusLabel,
    user,
    authAxios,
    onVersionStatusChange,
    showMyEntries,
    setShowMyEntries,
    hiddenCols,
    toggleCol,
    applyColPreset,
    showTransferColumns,
    showSyncControls,
    isSyncRefreshing,
    lastSyncedAt,
    lastConflictAt,
    autoRefreshEnabled,
    setAutoRefreshEnabled,
    onRefreshNow,
  };
}

export function buildLedgerTabsProps({
  showCombinedTypeView,
  setViewType,
  viewType,
  incTotal,
  expTotal,
}) {
  return {
    showCombinedTypeView,
    setViewType,
    viewType,
    incTotal,
    expTotal,
  };
}

export function buildLedgerSheetProps({
  showCombinedTypeView,
  viewType,
  tree,
  version,
  isVersionEditable,
  setIsAddOpen,
  selectedScopeOrgId,
  hierarchyActionRefs,
  openHierarchyActionId,
  closeHierarchyAction,
  openHierarchyAction,
  openHierarchyCommentPanel,
  commentUnresolvedLoaded,
  unresolvedByEntryId,
  unresolvedBySubjectId,
  unresolvedRootTypes,
  handleToggleCommentEntry,
  renderMokActions,
  addDetailRow,
  deleteDetail,
  deletingDetailId,
  localDetails,
  updateLocalDetail,
  flushAutoSave,
  authAxios,
  onRefresh,
  onRefreshSubjects,
  onRefreshProjects,
  supportingDocsSubjectIds,
  subjects,
  modalApi,
  hiddenCols,
  toggleCol,
}) {
  return {
    showCombinedTypeView,
    viewType,
    tree,
    version,
    isVersionEditable,
    setIsAddOpen,
    selectedScopeOrgId,
    hierarchyActionRefs,
    openHierarchyActionId,
    closeHierarchyAction,
    openHierarchyAction,
    openHierarchyCommentPanel,
    commentUnresolvedLoaded,
    unresolvedByEntryId,
    unresolvedBySubjectId,
    unresolvedRootTypes,
    handleToggleCommentEntry,
    renderMokActions,
    addDetailRow,
    deleteDetail,
    deletingDetailId,
    localDetails,
    updateLocalDetail,
    flushAutoSave,
    authAxios,
    onRefresh,
    onRefreshSubjects,
    onRefreshProjects,
    supportingDocsSubjectIds,
    subjects,
    modalApi,
    hiddenCols,
    toggleCol,
  };
}

export function buildLedgerOverlayProps(params) {
  return { ...params };
}
